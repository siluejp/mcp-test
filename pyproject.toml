[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp-test"
version = "0.1.0"
dependencies = [
    "mcp[cli]",
    "python-dotenv",
    "google-api-python-client",
]
